name: project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_guid: ^0.3.1
  google_fonts: ^6.2.1
  http: ^1.2.2
  image_picker: ^1.1.2 
  cupertino_icons: ^1.0.0
  flutter_svg_provider:
  
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/images/
